branches:
  only:
    - master
gemfile:
  - Gemfile
language: ruby
matrix:
  allow_failures:
    - rvm: ruby-head
  include:
    - env: COVERAGE=1
      gemfile: Gemfile
      rvm: 2.6.5
rvm:
  - 2.6.5
  - ruby-head
script: bundle exec rspec
